apply plugin: "java"

repositories {
  mavenRepo url: "file:///Users/${System.getProperty("user.name")}/Sites/mavenrepo"
}

dependencies {
  compile "org.conflictresolution:foo:1.0", "org.conflictresolution:bar:1.0"
}

task show << {
    configurations.compile.each {
      println it
    }
}