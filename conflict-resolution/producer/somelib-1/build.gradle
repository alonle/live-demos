apply plugin: "java"
apply plugin: "maven"

archivesBaseName = "baz"
version = "3"

uploadArchives {
  repositories {
    mavenDeployer {
      repository(url: "file:///Users/${System.getProperty("user.name")}/Sites/mavenrepo")
    }
  }
}